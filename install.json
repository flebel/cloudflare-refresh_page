{
  "resources": {
    "body": [
      {
        "type": "script",
        "src": "./source/app.js"
      }
    ]
  },
  "preview": {
    "handlers": [
      {
        "options": ["_default"],
        "execute": "INSTALL_SCOPE.setOptions(INSTALL_OPTIONS)"
      }
    ]
  },
  "options": {
    "properties": {
      "refresh_interval": {
        "title": "Refresh interval",
        "description": "How many seconds to wait before refreshing the page after it loaded.",
        "order": 0,
        "type": "integer",
        "default": 15
      }
    }
  }
}
